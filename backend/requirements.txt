# --- core (always required) ---
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
python-dotenv>=1.0
scipy>=1.11
networkx>=3.2
vaderSentiment>=3.3.2
feedparser>=6.0
httpx>=0.27
python-multipart>=0.0.9
beautifulsoup4>=4.12
rdflib>=7.0
numpy>=1.26
pypdf>=4.0
reportlab>=4.0

# --- auth ---
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3
email-validator>=2.0

# --- worldwide NLP (optional; app falls back to lexicon/regex if absent) ---
# Transformer biomedical NER
torch>=2.2            # install CPU build: pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.40
# Stage-4 MedDRA semantic map (optional; n-gram cosine fallback if absent)
# sentence-transformers>=2.6  # all-MiniLM-L6-v2; pip install when GPU/CPU budget allows
# Morphological fuzzy collapse (optional; token-Jaccard fallback if absent)
rapidfuzz>=3.6
# Pass-2 SapBERT/BioBERT ANN (optional; n-gram + numpy ANN if absent)
# sentence-transformers>=2.6
# faiss-cpu>=1.7.4
# Optional biomedical tokenizer / Pass-3 POS (scispaCy / spaCy); app runs without it
# spacy>=3.7
# scispacy>=0.5
# en_core_sci_sm  # pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_sm-0.5.4.tar.gz
# Multi-locale PII
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
# Playwright for login-walled source onboarding (optional):
# playwright>=1.40
# After install: playwright install chromium

# Translation + language detection
deep-translator>=1.11
langdetect>=1.0.9

# --- proactive risk strata (optional; NumPy IRLS logistic is the default) ---
# scikit-learn>=1.4
# lightgbm>=4.0
# shap>=0.44
